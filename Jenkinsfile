pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Yapılacak adımları buraya ekleyin (Örneğin, derleme işlemleri)
                echo 'Build step executed!'
            }
        }
        stage('Test') {
            steps {
                // Test adımlarını buraya ekleyin
                echo 'Test step executed!'
            }
        }
        // Diğer aşamaları ekleyebilirsiniz.
    }
}
